# OpenOnDemand Mode Configuration Example
# This file shows the environment variables needed to run the SSH Key Service
# in OpenOnDemand mode where authentication is handled externally.

# Enable OpenOnDemand mode (set to "true" to enable, "false" or unset for Azure OIDC mode)
USE_OPENONDEMAND=true

# Path to the file containing the OIDC token
# This token is provided by the OpenOnDemand platform
OIDC_TOKEN_PATH=/var/run/secrets/oidc/token

# Flask secret key for session management
# Generate a secure random key for production use:
# python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Base URL for the SSH Key Service API
AZURE_API_BASE_URL=http://localhost:8000
