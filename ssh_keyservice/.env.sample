# OpenOnDemand SSH Key Service Configuration
# Set these environment variables to configure the application

# Path to the OIDC token file provided by OpenOnDemand
# Default: /var/run/secrets/oidc/token
OIDC_TOKEN_PATH=/var/run/secrets/oidc/token

# Base URL for the SSH Key Service API backend
# Default: http://localhost:8000
API_BASE_URL=http://localhost:8000

# Flask secret key for session management
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# If not set, a random key will be generated on startup
FLASK_SECRET_KEY=your-secret-key-here
